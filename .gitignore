node_modules
dist
dist-ssr
*.local
.env
.env.local
.env.*.local
!.env.production
.vercel
coverage
playwright-report
test-results
.DS_Store
*.tsbuildinfo
incoming-assets

.vercel
.env*

# 승인 에셋 원본(ZIP)과 임시 해제 폴더 — Git 추적 제외
asset-drop/
.tmp-assets/
incoming-assets/

# Vite 빌드 산출물이 public/ 로 새어 들어오는 것을 막습니다.
# public/ 은 그대로 dist/ 로 복사되므로, 여기 남은 번들은 배포마다 함께 나갑니다.
public/assets/*.js
public/assets/*.css

# 프로토타입 단독 시연본은 빌드 산출물입니다. 원본은 index.html 입니다.
prototypes/**/standalone.html
prototypes/**/artifact.html
# Supabase anon key 가 들어가는 자리. config.example.js 를 복사해서 씁니다.
prototypes/shared/config.js

# 세션 중 만든 임시 검증 스크립트
_*.mjs
