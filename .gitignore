node_modules
dist
dist-ssr
*.local
.env
.env.local
.env.*.local
!.env.production
.vercel
coverage
playwright-report
test-results
.DS_Store
*.tsbuildinfo
incoming-assets

.vercel
.env*

# 승인 에셋 원본(ZIP)과 임시 해제 폴더 — Git 추적 제외
asset-drop/
.tmp-assets/
incoming-assets/

# Vite 빌드 산출물이 public/ 로 새어 들어오는 것을 막습니다.
# public/ 은 그대로 dist/ 로 복사되므로, 여기 남은 번들은 배포마다 함께 나갑니다.
public/assets/*.js
public/assets/*.css
