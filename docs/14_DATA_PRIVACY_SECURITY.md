# DATA, PRIVACY & SECURITY

## 1. 기본 원칙

- 온디바이스 자세 처리
- 영상·사진·프레임 미저장
- 영상·사진·프레임 미전송
- 개인 기준 대비 상대 변화
- 최소 데이터
- 사용자 삭제 가능
- 친구에게 성공 이벤트만 공유
- 의료 진단 아님

## 2. 데이터 분류

### 로컬 저장

| 데이터 | 용도 |
|---|---|
| 닉네임 | 개인화·친구 방 표시 |
| 개인 기준 요약 | 자세 비교 |
| 학습 프로필 | 환경별 설정 |
| 피드백 설정 | 소리·민감도·효과 |
| 세션 요약 | 기록·결과 |
| XP·포인트·출석 | 성장·상점 |
| 장착 아이템 | 캐릭터 표시 |
| 최근 스트레칭 | 반복 방지 |

### Supabase 저장

| 데이터 | 용도 |
|---|---|
| 익명 user id | 방 권한 |
| room id·code | 방 식별 |
| 닉네임 | 참가자 표시 |
| 방 상태 | waiting·running 등 |
| 보스 HP | 공동 게임 |
| 참가자 상태 | ready·focusing 등 |
| 생성·시작·종료 시각 | 방 수명주기 |

### Realtime 이벤트

- recovery_earned
- stretch_completed
- session_completed
- reaction_sent

### 저장·전송 금지

- 카메라 영상
- 사진·스냅샷
- 얼굴 이미지
- 프레임별 랜드마크
- 원본 자세 좌표
- bad 상태
- 나쁜 자세 지속 시간
- 통증·질환·건강 정보
- 의료적으로 해석될 점수

## 3. 카메라 생명주기

```text
권한 안내
→ 사용자 CTA
→ getUserMedia
→ 브라우저 메모리 처리
→ 세션·캘리브레이션 종료
→ 모든 media track stop
```

탭 이동, 카메라 해제, 권한 철회, 장치 변경에 대응합니다.

## 4. 의료 표현

### 허용

- 개인 기준
- 자세 변화
- 회복
- 감지 가능 시간
- 오래 앉은 흐름
- 일반 움직임 리셋

### 금지

- 진단
- 정상·비정상
- 거북목 확률
- CVA 결과
- 치료·교정 보장
- 통증 예방
- 특정 근육 문제 감지

## 5. 익명 인증

- 이메일·전화번호 수집 없음
- 익명 사용자는 Supabase에서 authenticated 역할 사용
- JWT의 사용자 ID로 방 접근을 제한
- 익명 사용자 남용 방지를 위해 rate limit·CAPTCHA를 검토
- 오래된 익명 사용자 정리 정책을 운영 환경에서 검토

## 6. RLS 원칙

- 모든 방 테이블 RLS 활성화
- room_members에 포함된 사용자만 방 읽기
- 방장 또는 멤버만 제한된 상태 변경
- 클라이언트 damage 직접 덮어쓰기 금지
- 이벤트 payload 서버·클라이언트 양쪽 검증

## 7. 환경 변수

프론트에 허용:

- Supabase URL
- Supabase publishable key

프론트 금지:

- secret key
- service role key
- 관리자 토큰

## 8. 닉네임

- 최대 12자
- HTML escape
- 제어 문자 제거
- 빈 값 기본 닉네임
- 욕설·공격적 표현 필터는 후속 검토
- 실명 입력 유도 없음

## 9. 데이터 초기화

사용자가 한 번에 삭제할 수 있어야 합니다.

- 닉네임
- 캘리브레이션
- 프로필
- 세션
- XP·포인트·출석
- 아이템
- 설정

친구 방의 서버 데이터는 방 종료 후 보존 기간을 짧게 설정하거나 정리 작업을 둡니다.

## 10. 보안 체크

- CSP 검토
- HTTPS
- 의존성 취약점 확인
- XSS 방지
- 방 코드 brute force 제한
- Realtime private channel
- payload 크기 제한
- 중복 이벤트 ID
- 저장 스키마 검증
- 오류 로그에 자세 데이터 금지

## 11. 출시 차단

- 카메라 데이터 네트워크 전송
- API secret 노출
- RLS 미설정
- 친구에게 bad 상태 공개
- 자세 점수 건강 표현
- 스트레칭 강제
- 데이터 초기화 미작동

## 12. 사용자 요청형 AI 세션 회고

- 결과 화면에서 사용자가 체크박스로 동의하고 `AI 회고 만들기`를 누를 때만 호출한다.
- Gemini 요청은 `plannedMinutes`, `elapsedMinutes`, `detectableMinutes`, `awayMinutes`, `recoveryOpportunities`, `recoveries`, `bestCombo`, `status`의 8개 집계만 허용한다.
- 세션 ID, 닉네임, 과목, 목표 문구, 카메라 영상·사진·스냅샷, 랜드마크·좌표, 원본 자세 상태, 나쁜 자세 지속 시간, 건강 정보는 요청에 포함하지 않는다.
- Gemini Interaction은 `store: false`로 호출하고, 서버의 Zod 스키마 검증과 의료 표현 금지 검사를 통과한 JSON만 화면·로컬 기록에 남긴다.
- Langfuse를 설정해도 프롬프트, 요청 집계값, 리포트 원문, 사용자·세션 식별자는 보내지 않는다. 모델명, 스키마 버전, 성공·실패, 지연 시간만 관찰한다.
- `GEMINI_API_KEY`, `LANGFUSE_SECRET_KEY`는 Vercel 서버 환경 변수로만 둔다. `VITE_` 접두어를 붙이지 않는다.
- 닉네임 XSS 가능
